# morai_ros_morai_msgs

# 워크스페이스  ~/aim_ws
cd ~/aim_ws/src
git clone https://github.com/MORAI-Autonomous/MORAI-ROS_morai_msgs.git
cd ..
rosdep install --from-paths src --ignore-src -r -y
catkin_make
source devel/setup.bash



# --------------------------------------

# geo_convert

# 0.
pip install pyproj pymap3d geographiclib utm

# 1.
catkin_create_pkg geo_convert \
  roscpp rospy std_msgs sensor_msgs geometry_msgs \
  tf2 tf2_ros tf2_geometry_msgs \
  geographic_msgs

# 2. 
sudo apt-get update
sudo apt-get install -y \
  libgeographiclib-dev geographiclib-tools \
  ros-noetic-geodesy
# (선택) robot_localization을 함께 쓸 계획이면:
sudo apt-get install -y ros-noetic-robot-localization
